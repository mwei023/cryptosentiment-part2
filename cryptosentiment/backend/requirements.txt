# Backend dependencies for CryptoSentiment.
# Install from the backend/ directory:  pip install -r requirements.txt
#
# NOTE: versions are intentionally unpinned — there is no lockfile yet.
# A fresh install resolves current compatible releases. Introduce a lockfile
# (pip-tools / uv) before any production deployment.

# --- API layer ---
fastapi
uvicorn
gunicorn

# --- Database ---
sqlalchemy
psycopg2-binary

# --- Background jobs (Celery + Redis broker) ---
celery
redis

# --- Data / ML ---
pandas
numpy
prophet
transformers
torch

# --- HTTP clients & config ---
requests
httpx
python-dotenv
