# Root-level convenience file.
#
# The backend's real dependency list lives in backend/requirements.txt
# (this file previously contained an accidental `pip freeze` dump of an
# unrelated virtualenv — httpx's transitive deps plus git-filter-repo —
# and could not run the project).
#
# Installing from the repo root works via this pointer, but the
# documented path is to install from backend/:
#
#     cd backend
#     pip install -r requirements.txt

-r backend/requirements.txt
